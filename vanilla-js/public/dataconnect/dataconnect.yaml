specVersion: "v1beta"
serviceId: "vanilla-js"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vanilla-js-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
